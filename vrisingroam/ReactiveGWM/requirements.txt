# Core
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24
Pillow>=9.0

# Model loading & inference
safetensors>=0.4.0
transformers>=4.36
einops>=0.7
tqdm>=4.65

# Text-prompt cleaning (negative prompt is bit-identical to training only with ftfy)
ftfy>=6.1

# Action parquet I/O & video writing
pandas>=2.0
pyarrow>=12.0
imageio>=2.31
imageio-ffmpeg>=0.4.9

# Optional: enables Flash-Attention 2 path in models/dit.py for ~2x faster attention
# Install only if your GPU + CUDA setup supports it
# flash-attn>=2.5.0
