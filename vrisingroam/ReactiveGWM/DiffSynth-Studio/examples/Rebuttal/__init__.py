"""Rebuttal-only ReactiveGWM training experiments.

All implementation in this package is intentionally isolated from the existing
``examples/ReactiveGWM`` training code.  That code is imported as a read-only
dependency where useful, but is never modified by the rebuttal experiments.
"""
